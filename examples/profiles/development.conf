# development — full-featured sandbox for coding tasks
# Usage: claudebox.sh --profile development --workdir ~/projects/myapp
--snapshot
--seccomp
--token-limit 500000
--timeout 60
--mem-limit 8G
--cpu-limit 400
