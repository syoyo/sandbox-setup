# analysis — read-only analysis with output directory
# Usage: claudebox.sh --profile analysis --workdir ~/projects/myapp --output-dir ~/analysis-out
--read-only-workdir
--seccomp
--mem-limit 4G
--idle-timeout 20
